/**
 * Build configuration - shared constants for build output locations
 * 
 * This file serves as the single source of truth for build output paths.
 * Update this file to change where built files are output, and all other
 * files will automatically use the new location.
 */

/**
 * Relative path from docs directory to the build output directory
 * This is where compiled JavaScript files are written during the build process
 */
export const BUILD_OUTPUT_DIR = 'public/examples';

